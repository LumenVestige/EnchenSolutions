//
// Created by sanenchen on 25-9-21.
//
#include <bits/stdc++.h>
using namespace std;
char findMin(string s) {
    char m = 'A';
    for (auto& i : s) {
        m = min(m, i);
    }
    return m;
}
int main() {
    string s;
    int k;
    cin >> s >> k;
    for (int i = 0; i < s.size(); ++i) {
        if (k)
    }
    return 0;
}