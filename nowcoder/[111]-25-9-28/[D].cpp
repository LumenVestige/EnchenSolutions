#include <bits/stdc++.h>
using namespace std;
#define int long long
signed main() {
    int n;
    cin >> n;
    // 预先处理
    vector<pair<int, pair<int, int>>> cnt;
    vector<int> nums(n);
    for (int i = 0; i < n; ++i) {
        cin >> nums[i];
        // 获取
    }

    return 0;
}